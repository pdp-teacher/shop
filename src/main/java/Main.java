import order.Order;
import order.OrderRepository;
import payment.Payment;
import payment.PaymentRepository;
import products.Products;
import products.ProductsRepository;
import user.User;
import user.UserRepository;

import java.util.UUID;

public class Main {
    public static void main(String[] args) {
        // OrderRepository orderRepository = new OrderRepository();
        // orderRepository.add(new Order(UUID.randomUUID(),UUID.randomUUID(),UUID.randomUUID()));
        //     orderRepository.getAll().forEach(System.out::println);
        //UserRepository userRepository = new UserRepository();
        // User add = userRepository.add(new User());
        //userRepository.getAll().forEach(System.out::println);
//        PaymentRepository paymentRepository = new PaymentRepository();
//        paymentRepository.add(new Payment());
//        paymentRepository.getAll().forEach(System.out::println);
//        ProductsRepository productsRepository = new ProductsRepository();
//        productsRepository.add(new Products());
//        productsRepository.getAll().forEach(System.out::println);


    }
}
